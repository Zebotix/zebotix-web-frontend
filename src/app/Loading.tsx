import LoadingComponent from '@/components/ui/LoadingComponent';
import React from 'react';

const Loading = () => {
  return (
    <div>
      <LoadingComponent size='3xl' />
    </div>
  );
};

export default Loading;
